# Provisional GitHub Pages deployment — kept fully closed to crawlers on purpose.
# The production deployment is on Aruba (build-aruba.sh generates a separate,
# indexable robots.txt for that build). This file only applies to the
# GitHub Pages staging copy.
User-agent: *
Disallow: /
