# Provisional GitHub Pages deployment — kept fully closed to crawlers on purpose.
# When the site goes live on its final domain: change Disallow to just the paths
# that should stay out of the index (if any), remove the noindex meta tag in
# index.html, and uncomment the Sitemap line below (updated to the final domain).
User-agent: *
Disallow: /

# Sitemap: https://www.YOUR-FINAL-DOMAIN.com/sitemap.xml
